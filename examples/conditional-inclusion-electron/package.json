{
  "name": "@jam/conditional-inclusion-electron",
  "version": "0.1.0",
  "description": "Electron wrapper for conditional-inclusion demo",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && electron-builder --dir",
    "build:web": "cd ../conditional-inclusion && bun run build",
    "package": "bun run build:web && bun run build && electron-builder",
    "package:mac": "bun run build:web && bun run build && electron-builder --mac",
    "package:win": "bun run build:web && bun run build && electron-builder --win",
    "package:linux": "bun run build:web && bun run build && electron-builder --linux"
  },
  "dependencies": {
    "electron-is-dev": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "typescript": "^5.8.3",
    "vite": "^7.0.5",
    "vite-plugin-electron": "^0.28.0",
    "vite-plugin-electron-renderer": "^0.14.0"
  },
  "build": {
    "appId": "com.jam.conditional-inclusion-electron",
    "productName": "Jam Conditional Inclusion Demo",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "web-dist/**/*"
    ],
    "protocols": {
      "name": "jam-electron-demo",
      "schemes": ["jam-electron-demo"]
    },
    "mac": {
      "category": "public.app-category.developer-tools",
      "target": ["dmg", "zip"]
    },
    "win": {
      "target": ["nsis", "portable"]
    },
    "linux": {
      "target": ["AppImage", "deb"],
      "category": "Development"
    }
  }
}
